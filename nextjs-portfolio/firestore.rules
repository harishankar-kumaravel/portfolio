rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Only allow public read access, all edits/writes require admin authentication
    match /portfolio/{document} {
      allow read: if true;
      allow write: if request.auth != null;
    }
  }
}
